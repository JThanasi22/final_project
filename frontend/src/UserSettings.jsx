import React from 'react';

const UserSettings = () => {
    return (
        <div style={{ padding: '25px' }}>
            <h1>User Settings</h1>
            <p>Manage your profile, account, and preferences here.</p>
        </div>
    );
};

export default UserSettings;